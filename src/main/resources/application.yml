#To Change from /camel to / in url
camel:
  springboot:
    name: LoanAPIs
  servlet:
    mapping:
      context-path: /*
  rest:
    context-path: /
    
server:
  port: 8081
  
loanAPI:
  getCustomerLoanDetailsHost : ${LOAN_DB_GET_CUSTOMER_LOAN_DETAILS_HOST}
  getCustomerLoanDetailsContextPath : ${LOAN_DB_GET_CUSTOMER_LOAN_DETAILS_CONTEXTPATH}
  newLoanApplicationHost : ${LOAN_DB_NEW_LOAN_APPLICATION_HOST}
  newLoanApplicationContextPath : ${LOAN_DB_NEW_LOAN_APPLICATION_CONTEXTPATH}